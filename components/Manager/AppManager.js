import React, { Component, PropTypes } from 'react';
import ReactDOM from 'react-dom';
import styles from './AppManager.css'

const AppManager = () => {
    return (
        <div className={ styles.root }>
            NodeUI
        </div>
    );
};

export default AppManager;
